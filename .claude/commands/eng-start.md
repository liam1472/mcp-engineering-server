---
description: Start working on a new feature
allowed-tools: MCP
---

Run the MCP tool `eng_start` with feature name from arguments: $ARGUMENTS

Usage: /eng-start <feature-name>

This will:
1. Create feature directory `.engineering/features/<name>/`
2. Initialize feature manifest
3. Track feature progress

Example: /eng-start user-authentication
