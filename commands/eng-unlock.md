---
description: Unlock a previously locked file
allowed-tools: MCP
---

Run the MCP tool `eng_unlock` with file path from arguments: $ARGUMENTS

Usage: /eng-unlock <file-path>

Releases the lock on a file so other sessions can use it.

Example: /eng-unlock src/index.ts
