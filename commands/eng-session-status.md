---
description: View all active sessions and locked files
allowed-tools: MCP
---

Run the MCP tool `eng_session_status` to view session status.

Shows:
- Active sessions (A, B, C)
- Current session marker
- Files locked by each session
